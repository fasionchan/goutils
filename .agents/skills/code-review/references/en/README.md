# English references (placeholder)

English chapter tree is reserved for a later iteration.

For the first release, agents must load only `references/zh/` and must not treat this directory as substantive review guidance.
